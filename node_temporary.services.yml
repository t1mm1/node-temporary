services:
  node_temporary.helper:
    class: Drupal\node_temporary\NodeTemporaryHelper
    arguments:
      - '@entity_type.manager'
      - '@messenger'
      - '@current_user'
      - '@request_stack'
      - '@config.factory'
    public: true
  node_temporary.queue:
    class: Drupal\node_temporary\NodeTemporaryQueue
    arguments:
      - '@entity_type.manager'
      - '@queue'
    public: true

  Drupal\node_temporary\NodeTemporaryHelper:
    alias: 'node_temporary.helper'
  Drupal\node_temporary\NodeTemporaryQueue:
    alias: 'node_temporary.queue'
